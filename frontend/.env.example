# Leave unset in development: Vite proxies /api to the FastAPI process.
# Set it when the API lives on a different origin in a deployed environment.
# VITE_API_BASE_URL=https://api.example.com/api/v1
